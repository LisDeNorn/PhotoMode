package com.photomode.domain.model

/**
 * In-app content and UI language. Persisted in the data layer; string resources follow via
 * AppCompat per-app locales in the presentation layer.
 */
enum class AppLocale {
    ENGLISH,
    RUSSIAN
}
